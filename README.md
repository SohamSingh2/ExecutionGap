# ExecutionGap
Execution Gap Agent is a Python accountability app with CLI and web UI for tracking goals, concrete tasks, daily execution, avoidance patterns, journal entries, and weekly resets. It stores data in SQLite, supports interactive task check-ins, and can run locally or be deployed as a Python web service.
